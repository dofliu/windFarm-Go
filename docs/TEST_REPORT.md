# 系統測試報告 — Full-System Verification

> 目的:記錄本專案「全功能邏輯/程式 + 壓力測試」的**測試項目、方法、結果**,作為發布前的驗證紀錄與可重現依據。
> 最近一次完整執行:**2026-09-01**(commit `a954883` 之後的工作樹,新增 Playwright UI 迴歸測試)。
> 一鍵重跑:`npm run typecheck && npm test && npm run build && npm run sim && npm run stress`(線上後端另見第 6 節;UI 迴歸見第 7 節)。

---

## 摘要 — 結果一覽

| # | 測試層面 | 指令 | 項目數 | 結果 |
|---|---|---|---|---|
| 1 | 型別檢查 | `npm run typecheck` | 全專案 | ✅ 零錯誤 |
| 2 | 生產建置 | `npm run build` | bundle 產出 | ✅ 成功(初始包 < 500KB) |
| 3 | 邏輯/程式測試 | `npm test` | **167** | ✅ 全過 |
| 4 | 平衡模擬 | `npm run sim` | 3 策略 × 120 天 | ✅ 進程激勵單調、無崩盤 |
| 5 | 併發壓力測試 | `npm run stress` | 3 情境 + 競態示範 | ✅ 契約守住、鎖有效 |
| 6 | 線上後端實打 | `npm run live-check` | 10 項 | ⚙️ 需本機執行(沙盒代理擋 `script.google.com`) |
| 7 | Playwright UI 迴歸 | `npm run build && npm run e2e` | 7 項 | ✅ 全過(CI 新增 `e2e` job) |

---

## 1. 型別檢查(TypeScript)

- **項目**:全 `src/` 與 `test/` 載入模組的靜態型別正確性。
- **方法**:`tsc --noEmit`,以 `tsconfig.json` 嚴格設定編譯但不輸出。
- **結果**:✅ 零型別錯誤。

## 2. 生產建置(Vite)

- **項目**:正式打包可成功產出,且程式碼分割(code-split)後初始載入量受控。
- **方法**:`tsc -b && vite build`。
- **結果**:✅ 建置成功。代表性 chunk:

  | chunk | 大小 | gzip |
  |---|---|---|
  | `index`(初始) | 319 KB | 114 KB |
  | `vendor`(React 等) | 142 KB | 45.5 KB |
  | `tasks`(任務題庫) | 128 KB | 46.9 KB |
  | 各 Modal/Screen | 3–30 KB | 1.5–9.7 KB(延遲載入) |

  皆低於 500KB 警示門檻;重型畫面(OpsCenter/Repair/Facility 等)以 `React.lazy` 延遲載入。

## 3. 邏輯/程式測試 — `npm test`(167 項全過)

- **方法**:`test/run.mjs` 以 esbuild 即時打包各 TS 模組,於 Node 內以自製極簡測試框架(`ok/eq/near/seed`)斷言。`seed()` 注入決定性亂數,使含隨機性的邏輯可重現。
- **涵蓋範圍**(依模組分區):

  | 區塊 | 代表測試 | 驗證重點 |
  |---|---|---|
  | 核心循環/經濟 | 日推進、售電收入、倉儲折舊、薪資/疲勞、SLA 結算 | 收支與時間成本正確、可用率單一真實來源 |
  | 戰情室/派工 | 科別配對派工、遠端重啟、船舶並行上限、出海動員費、定檢 buff | 派工規則、批次出海不重複收費、備品消耗 |
  | Tier 漸進難度 | `tierOf` 單調、故障/備品/事件池隨 tier 成長 | 入門只見簡單故障,規模擴大才解鎖 |
  | 圖鑑/多重根因 | 每故障五欄圖鑑、元件 1:1 對應、同元件症狀互異 | 鑑別診斷題組可成立 |
  | 判斷任務引擎 | 192 題格式/平衡/可重現、七類各 ≥15 題 | 每題有好/壞選項與雙語回饋 |
  | **HSE 加厚(#2)** | LOTO/墜落防護/SIMOPS 故障、戰情室事件 | HSE 科別、Tier 2、備品同調、多重根因 |
  | **真實權衡(#3)** | 直升機進場、電網限電 | 每題為真實取捨(好+壞選項),負電價最佳解 = 降載避免倒貼 |
  | 知識點掌握度 | `recordAnswer` 累計、`weakest` minN 門檻、reducer | 正確率統計與弱點判定 |
  | 真實案例研究 | 完整性、Tier 分層、具名引用出處/去識別不洩名、relatesTo 連結 | 真實案例導入的資料防護 |
  | PWA | manifest/SW/index 配線、不快取雲端 API | 可安裝/離線、線上資料不被快取污染 |
  | 教師面板/趨勢/存檔 | CSV(BOM/CRLF/跳脫)、趨勢彙整、`migrateSave` 版本遷移 | 匯出格式、賽後復盤、舊存檔相容 |
  | 健壯性 | **fuzz:3000 次隨機動作** | 永不產生非法狀態、永不拋例外 |

- **結果**:`167 passed, 0 failed`。

## 4. 平衡模擬 — `npm run sim`(120 天)

- **項目**:驗證「投入越多→成效越好」的進程激勵設計成立,且長線不崩盤。
- **方法**:`test/sim.mjs` 以三種玩法策略各推進 120 遊戲日,量測妥善率、停機損失與績效分。
- **結果**(代表性):

  | 策略 | 妥善率(現/均/低) | 停機損失 MWh | 績效分 |
  |---|---|---|---|
  | passive(放任) | 58 / 87 / 54 % | 1965 | 12145 |
  | active(管理) | 79 / 94 / 71 % | 875 | 13771 |
  | full-crew(全配技師) | 100 / 97 / 83 % | 465 | **14961** |

  → 三策略績效**單調遞增**(放任 < 管理 < 全配),且放任策略仍不崩盤(均妥善率 87%),符合「入門友善、精進有回報」的設計。

## 5. 併發壓力測試 — `npm run stress`

- **項目**:模擬「多位學生同時提交成績/存檔、讀取排行榜」,檢驗雲端後端契約在併發下是否守住,以及併發鎖是否消除競態。
- **方法**:`test/stress.mjs` 在本機**忠實重現** Google Apps Script 後端契約(djb2 簽章驗證、`clampInt` 夾值、8 秒節流去重、排行榜彙整、同時執行上限以 semaphore 模擬「排隊而非丟棄」),再以併發負載打它。**不直接壓測線上 `/exec`**(避免踩 Google 配額、不對線上服務灌流量)。詳見 [`STRESS_TEST.md`](STRESS_TEST.md)。
- **結果**:

  | 情境 | 吞吐量 | 延遲 p50/p95/p99 | 簽章錯 | 其他錯 | 同時執行 | 最大排隊 | 判定 |
  |---|---|---|---|---|---|---|---|
  | 50 人 / 10s / cap 30(單班真實) | 706 req/s | 68 / 97 / 104 ms | 0 | 0 | 30(=上限) | 20 | ✅ |
  | 200 人 / 10s / cap 30(預設) | 701 req/s | 284 / 315 / 348 ms | 0 | 0 | 30 | 170 | ✅ |
  | 500 人 / 12s / cap 30(超載) | 747 req/s | 712 / 759 / 913 ms | 0 | 0 | 30 | 470 | ✅ |

  **關鍵觀察**
  1. 超過後端同時執行上限的請求**排隊而非丟棄**(無 5xx、無遺失)。
  2. 簽章驗證、`clampInt` 夾值、8 秒去重(防作弊/省寫入)在併發下全部正確;`bad-sig = 0`。
  3. 延遲隨飽和**優雅升高**(p99 由 ~0.1s → ~0.9s),系統穩定不崩。對「送分/讀排行」這類非互動關鍵路徑,秒級延遲可接受。

  **併發競態示範**(對應 `Code.gs` v2.1 `LockService`):200 個併發「讀-改-寫」同一筆紀錄

  | 條件 | 最終計數(應為 200) |
  |---|---|
  | 無鎖 | **1** ← lost updates,多人互相覆蓋 |
  | 有 `LockService`(Code.gs v2.1) | **200** ← 序列化、無遺失 |

  → 確認雲端併發鎖有效消除了無鎖路徑會發生的更新遺失。

## 6. 線上後端實打 — `npm run live-check`

- **項目**:對**實際部署**的 Apps Script 後端做端到端檢查(註冊/登入/存檔/送分/讀排行 + 併發鎖證明,共 10 項)。
- **方法**:`test/live-check.mjs` 直接呼叫線上 Web App;預設唯讀,加 `--write` 才寫測試資料。
- **狀態**:⚙️ **本沙盒的出站代理會阻擋 `script.google.com`(403),無法於此環境執行**——這是環境網路限制,非程式問題。請於**本機**執行 `node test/live-check.mjs` 驗證(先前本機實跑 10 項全通,含併發鎖證明)。

## 7. Playwright UI 迴歸測試 — `npm run e2e`(7 項全過)

- **緣起**:先前每輪無障礙(鍵盤操作/彈窗 focus trap)改動僅在開發 session 內用 `playwright-core` 手動截圖/操作驗證(見 `docs/HANDOFF.md`「視覺驗證流程」),未沉澱為可重複執行、進 CI 把關的測試,迴歸風險需每次重新手動走一遍。本輪補上第一批**真正落地的自動化 UI 迴歸測試**。
- **方法**:`test/e2e.mjs` 用 Node 內建 `http` 模組把 `npm run build` 產出的 `dist/`(含 `/windFarm-Go/` 子路徑)在本機起一個靜態伺服器,再用 `playwright`(chromium)驅動真實瀏覽器操作、斷言。排行榜等雲端讀取(`script.google.com`)一律以 `context.route` 攔截中止,測試不打正式後端、不受網路狀況影響。
- **涵蓋範圍**:登入畫面載入 → 訪客登入進入母港 → 新手教學可跳過 → 開啟「調度中心」彈窗時 focus 移入面板 → **連續 14 次 Tab + 1 次 Shift+Tab 皆侷限於彈窗內(不逃逸到背景)** → `Esc` 關閉彈窗並把焦點歸還給觸發元件 → 整段流程無 console 錯誤/未捕捉例外。這组測試直接對應 PR #122/#123 新增的鍵盤操作與 focus trap 邏輯,把手動驗證過的行為轉為可重複執行的迴歸測試。
- **有效性驗證**:刻意暫時移除 `useFocusTrap.ts` 的 Tab 循環邏輯後重跑,測試如預期在「第 6 次 Tab 後 focus 逃出了彈窗」失敗,證明測試確實會抓到迴歸,而非空跑;還原後 7 項全過。
- **CI**:`.github/workflows/ci.yml` 新增獨立 `e2e` job(`npm ci` → `npm run build` → `npx playwright install --with-deps chromium` → `npm run e2e`),與既有 `check` job(typecheck/test/build)並行,兩者皆需全綠才能合併。
- **結果**:`7 passed, 0 failed`。
- **已知限制/後續**:目前僅覆蓋「調度中心」彈窗一個代表性樣本;其餘 11 個彈窗共用同一個 `useFocusTrap` hook,行為已由單元測試(`getFocusables`/`nextTrappedIndex`)與程式碼共用路徑保證一致,但尚未逐一端到端覆蓋。後續可比照擴充到交易所、維修畫面等其他核心互動流程。

---

## 結論

全功能邏輯(167 測試)、型別、建置、平衡模擬、併發壓力測試、Playwright UI 迴歸(7 測試)**全部通過**;併發鎖證實有效。唯一無法在沙盒內跑的是線上後端實打(代理限制),需於本機執行。**系統目前處於可發布狀態。**

> 維護備註:新增功能時請同步在 `test/run.mjs` 補對應測試(邏輯/reducer)或 `test/e2e.mjs`(UI 流程),並更新本報告的「最近一次執行」日期與對應涵蓋表。
