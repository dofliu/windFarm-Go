# 系統測試報告 — Full-System Verification

> 目的:記錄本專案「全功能邏輯/程式 + 壓力測試」的**測試項目、方法、結果**,作為發布前的驗證紀錄與可重現依據。
> 最近一次完整執行:**2026-09-10**(例行 session,無障礙補完:頂欄 TopBar/MobileBar 全數自訂互動改支援鍵盤操作 + 修正串接開啟彈窗的焦點歸還迴歸)。
> 一鍵重跑:`npm run typecheck && npm test && npm run build && npm run sim && npm run stress`(線上後端另見第 6 節;UI 迴歸見第 7 節)。

---

## 摘要 — 結果一覽

| # | 測試層面 | 指令 | 項目數 | 結果 |
|---|---|---|---|---|
| 1 | 型別檢查 | `npm run typecheck` | 全專案 | ✅ 零錯誤 |
| 2 | 生產建置 | `npm run build` | bundle 產出 | ✅ 成功(初始包 < 500KB) |
| 3 | 邏輯/程式測試 | `npm test` | **167** | ✅ 全過 |
| 4 | 平衡模擬 | `npm run sim` | 3 策略 × 120 天 | ✅ 進程激勵單調、無崩盤 |
| 5 | 併發壓力測試 | `npm run stress` | 3 情境 + 競態示範 | ✅ 契約守住、鎖有效 |
| 6 | 線上後端實打 | `npm run live-check` | 10 項 | ⚙️ 需本機執行(沙盒代理擋 `script.google.com`) |
| 7 | Playwright UI 迴歸 | `npm run build && npm run e2e` | 29 項 | ✅ 全過(CI `e2e` job) |

---

## 1. 型別檢查(TypeScript)

- **項目**:全 `src/` 與 `test/` 載入模組的靜態型別正確性。
- **方法**:`tsc --noEmit`,以 `tsconfig.json` 嚴格設定編譯但不輸出。
- **結果**:✅ 零型別錯誤。

## 2. 生產建置(Vite)

- **項目**:正式打包可成功產出,且程式碼分割(code-split)後初始載入量受控。
- **方法**:`tsc -b && vite build`。
- **結果**:✅ 建置成功。代表性 chunk:

  | chunk | 大小 | gzip |
  |---|---|---|
  | `index`(初始) | 319 KB | 114 KB |
  | `vendor`(React 等) | 142 KB | 45.5 KB |
  | `tasks`(任務題庫) | 128 KB | 46.9 KB |
  | 各 Modal/Screen | 3–30 KB | 1.5–9.7 KB(延遲載入) |

  皆低於 500KB 警示門檻;重型畫面(OpsCenter/Repair/Facility 等)以 `React.lazy` 延遲載入。

## 3. 邏輯/程式測試 — `npm test`(167 項全過)

- **方法**:`test/run.mjs` 以 esbuild 即時打包各 TS 模組,於 Node 內以自製極簡測試框架(`ok/eq/near/seed`)斷言。`seed()` 注入決定性亂數,使含隨機性的邏輯可重現。
- **涵蓋範圍**(依模組分區):

  | 區塊 | 代表測試 | 驗證重點 |
  |---|---|---|
  | 核心循環/經濟 | 日推進、售電收入、倉儲折舊、薪資/疲勞、SLA 結算 | 收支與時間成本正確、可用率單一真實來源 |
  | 戰情室/派工 | 科別配對派工、遠端重啟、船舶並行上限、出海動員費、定檢 buff | 派工規則、批次出海不重複收費、備品消耗 |
  | Tier 漸進難度 | `tierOf` 單調、故障/備品/事件池隨 tier 成長 | 入門只見簡單故障,規模擴大才解鎖 |
  | 圖鑑/多重根因 | 每故障五欄圖鑑、元件 1:1 對應、同元件症狀互異 | 鑑別診斷題組可成立 |
  | 判斷任務引擎 | 192 題格式/平衡/可重現、七類各 ≥15 題 | 每題有好/壞選項與雙語回饋 |
  | **HSE 加厚(#2)** | LOTO/墜落防護/SIMOPS 故障、戰情室事件 | HSE 科別、Tier 2、備品同調、多重根因 |
  | **真實權衡(#3)** | 直升機進場、電網限電 | 每題為真實取捨(好+壞選項),負電價最佳解 = 降載避免倒貼 |
  | 知識點掌握度 | `recordAnswer` 累計、`weakest` minN 門檻、reducer | 正確率統計與弱點判定 |
  | 真實案例研究 | 完整性、Tier 分層、具名引用出處/去識別不洩名、relatesTo 連結 | 真實案例導入的資料防護 |
  | PWA | manifest/SW/index 配線、不快取雲端 API | 可安裝/離線、線上資料不被快取污染 |
  | 教師面板/趨勢/存檔 | CSV(BOM/CRLF/跳脫)、趨勢彙整、`migrateSave` 版本遷移 | 匯出格式、賽後復盤、舊存檔相容 |
  | 健壯性 | **fuzz:3000 次隨機動作** | 永不產生非法狀態、永不拋例外 |

- **結果**:`167 passed, 0 failed`。

## 4. 平衡模擬 — `npm run sim`(120 天)

- **項目**:驗證「投入越多→成效越好」的進程激勵設計成立,且長線不崩盤。
- **方法**:`test/sim.mjs` 以三種玩法策略各推進 120 遊戲日,量測妥善率、停機損失與績效分。
- **結果**(2026-09-14 例行 session 重新實跑,確認內容修正——軸承備品正名/`crew_shortage` 折抵等——後回測仍健康):

  | 策略 | 妥善率(現/均/低) | 停機損失 MWh | 績效分 |
  |---|---|---|---|
  | passive(放任) | 50 / 65 / 50 % | 5050 | 8070 |
  | active(管理) | 83 / 93 / 83 % | 1105 | 13460 |
  | full-crew(全配技師) | 88 / 97 / 83 % | 535 | **14794** |

  → 三策略績效**單調遞增**(放任 ≪ 管理 < 全配),且放任策略仍不崩盤(均妥善率 65%),符合「入門友善、精進有回報」的設計。**絕對數字**因倉儲折舊迴圈依 `inventory` 插入順序擲骰(見 `CLAUDE.md`/`docs/HANDOFF.md`「模擬器蝴蝶效應」),隨備品 id 集合/順序異動而整段跳動,屬已知、非迴歸;回測只需確認**相對排序與梯度**健康,不追求絕對值穩定。此次重跑未發現排序或梯度異常,**免校正**。

## 5. 併發壓力測試 — `npm run stress`

- **項目**:模擬「多位學生同時提交成績/存檔、讀取排行榜」,檢驗雲端後端契約在併發下是否守住,以及併發鎖是否消除競態。
- **方法**:`test/stress.mjs` 在本機**忠實重現** Google Apps Script 後端契約(djb2 簽章驗證、`clampInt` 夾值、8 秒節流去重、排行榜彙整、同時執行上限以 semaphore 模擬「排隊而非丟棄」),再以併發負載打它。**不直接壓測線上 `/exec`**(避免踩 Google 配額、不對線上服務灌流量)。詳見 [`STRESS_TEST.md`](STRESS_TEST.md)。
- **結果**:

  | 情境 | 吞吐量 | 延遲 p50/p95/p99 | 簽章錯 | 其他錯 | 同時執行 | 最大排隊 | 判定 |
  |---|---|---|---|---|---|---|---|
  | 50 人 / 10s / cap 30(單班真實) | 706 req/s | 68 / 97 / 104 ms | 0 | 0 | 30(=上限) | 20 | ✅ |
  | 200 人 / 10s / cap 30(預設) | 701 req/s | 284 / 315 / 348 ms | 0 | 0 | 30 | 170 | ✅ |
  | 500 人 / 12s / cap 30(超載) | 747 req/s | 712 / 759 / 913 ms | 0 | 0 | 30 | 470 | ✅ |

  **關鍵觀察**
  1. 超過後端同時執行上限的請求**排隊而非丟棄**(無 5xx、無遺失)。
  2. 簽章驗證、`clampInt` 夾值、8 秒去重(防作弊/省寫入)在併發下全部正確;`bad-sig = 0`。
  3. 延遲隨飽和**優雅升高**(p99 由 ~0.1s → ~0.9s),系統穩定不崩。對「送分/讀排行」這類非互動關鍵路徑,秒級延遲可接受。

  **併發競態示範**(對應 `Code.gs` v2.1 `LockService`):200 個併發「讀-改-寫」同一筆紀錄

  | 條件 | 最終計數(應為 200) |
  |---|---|
  | 無鎖 | **1** ← lost updates,多人互相覆蓋 |
  | 有 `LockService`(Code.gs v2.1) | **200** ← 序列化、無遺失 |

  → 確認雲端併發鎖有效消除了無鎖路徑會發生的更新遺失。

## 6. 線上後端實打 — `npm run live-check`

- **項目**:對**實際部署**的 Apps Script 後端做端到端檢查(註冊/登入/存檔/送分/讀排行 + 併發鎖證明,共 10 項)。
- **方法**:`test/live-check.mjs` 直接呼叫線上 Web App;預設唯讀,加 `--write` 才寫測試資料。
- **狀態**:⚙️ **本沙盒的出站代理會阻擋 `script.google.com`(403),無法於此環境執行**——這是環境網路限制,非程式問題。請於**本機**執行 `node test/live-check.mjs` 驗證(先前本機實跑 10 項全通,含併發鎖證明)。

## 7. Playwright UI 迴歸測試 — `npm run e2e`(29 項全過)

- **緣起**:先前每輪無障礙(鍵盤操作/彈窗 focus trap)改動僅在開發 session 內用 `playwright-core` 手動截圖/操作驗證(見 `docs/HANDOFF.md`「視覺驗證流程」),未沉澱為可重複執行、進 CI 把關的測試,迴歸風險需每次重新手動走一遍。2026-09-01 補上第一批(7 項,登入/教學/彈窗 focus trap);2026-09-02 延伸覆蓋到交易所/出海/維修畫面(7 項);2026-09-03 再延伸覆蓋「作業窗吃緊 → 加班搶修(`#rush`)」分支路徑(2 項);2026-09-04 補上「風場戰情室」/「母港建設」兩個彈窗的 focus trap e2e 樣本(2 項);2026-09-05 再補上「營運趨勢」彈窗的 focus trap e2e 樣本(1 項);2026-09-06 再補上「課程模式」彈窗的 focus trap e2e 樣本(1 項);2026-09-07 再補上「機具工坊」(`FacilityModal` kind="tool")彈窗的 focus trap e2e 樣本(1 項);2026-09-08 補上「風場建置番外篇」(`ConstructionModal`)彈窗的鍵盤操作 e2e 樣本(1 項);2026-09-09 補上登入畫面(`LoginScreen`)的鍵盤操作 e2e 樣本(1 項);2026-09-10 補上頂欄(`TopBar`/`MobileBar`)鍵盤操作 + `ExamModal` 開始頁樣本(2 項);2026-09-11 補上「個人檔案」(`ProfileModal`)彈窗的 focus trap e2e 樣本(1 項);2026-09-12 補上「案例檔」(`CaseFileModal`)彈窗的 focus trap e2e 樣本(1 項);2026-09-13 補上「自由營運中心」(`OpsCenterModal`)判斷任務選項卡的鍵盤操作 e2e 樣本(1 項);本輪(2026-09-14)補上「獨立測驗模式」(`ExamModal`)作答頁/結果頁的完整測驗流程 e2e 樣本(1 項,見下)。
- **方法**:`test/e2e.mjs` 用 Node 內建 `http` 模組把 `npm run build` 產出的 `dist/`(含 `/windFarm-Go/` 子路徑)在本機起一個靜態伺服器,再用 `playwright`(chromium)驅動真實瀏覽器操作、斷言。排行榜等雲端讀取(`script.google.com`)一律以 `context.route` 攔截中止,測試不打正式後端、不受網路狀況影響。
- **涵蓋範圍(前 7 項,2026-09-01)**:登入畫面載入 → 訪客登入進入母港 → 新手教學可跳過 → 開啟「調度中心」彈窗時 focus 移入面板 → **連續 14 次 Tab + 1 次 Shift+Tab 皆侷限於彈窗內(不逃逸到背景)** → `Esc` 關閉彈窗並把焦點歸還給觸發元件。
- **涵蓋範圍(中 7 項,2026-09-02)**:延續同一個瀏覽器 session,走完首筆工單「齒輪箱搶修 CH-12」golden path 全程 ——
  1. 母港「待處理工單」接單(`ACCEPT_QUEST`)。
  2. **交易所(MarketScreen)**:鍵盤 `Enter` 觸發備品卡片加入購物車(`onKeyActivate`)、確認採購。
  3. **出海航行(SailScreen)**:出勤就緒檢查(船/技師/備品/天氣皆綠燈)通過後出航,等待航行動畫抵達機組。
  4. **維修(RepairScreen)**:海象平穩直接登船登塔;鍵盤 `Enter` 選擇診斷測驗正解選項;鍵盤 `Enter` 依序完成 3 個可互動 SOP 步驟;完工按鈕轉為可點擊、點擊後回母港、原警報解除。
  5. 全程再次確認無 console 錯誤/未捕捉例外。

  這批把「診斷測驗選項」與「SOP 步驟清單」的鍵盤觸發(PR #122 的 `onKeyActivate` 套用範圍)從單元測試(`getFocusables`/`nextTrappedIndex`/`onKeyActivate` 純函式層級)延伸為端到端驗證:鍵盤操作不僅「聚焦得到」,也真的能**推進遊戲狀態、完成工單**。對話框(`DialogueLayer`,`say()` 觸發的全螢幕 zIndex:50 遮罩)新增 `data-testid="dialogue-layer"` 供測試辨識、以共用的 `dismissDialogue()` 輔助函式逐句點掉。
- **新增涵蓋範圍(後 2 項,2026-09-03)**:延續同一次工單(CH-12)的維修流程,但改走「維修不利」的 Part B 分支——原本 golden path 一路答對診斷、從未觸發 `tightWindow` 提示,前輪 TEST_REPORT 已明列此分支為已知缺口:
  1. 在診斷測驗上**連續故意選錯兩個選項**(每次扣 3 時段,10 時段的作業窗耗到剩 4,低於估計所需 7),驗證「作業窗吃緊」提示如預期出現(繼續作業 / ⚡加班搶修 / 回港再規劃 三選一)。
  2. 點擊 **⚡加班搶修(`#rush`)**,驗證剩餘 3 個 SOP 步驟一次完成、吃緊提示解除;再答對診斷、完工按鈕轉為可點擊,完成整趟工單。`rush()` 的安全近失與否由前端 `Math.random() < RUSH_RISK`(25%)擲骰決定(reducer 只收布林值以保持可測),測試以 `page.evaluate` 暫時覆寫 `Math.random` 回傳固定值鎖定「無事件」分支,避免真實機率造成測試非決定性,用畢立即還原,不影響同頁面其他隨機邏輯(如天氣預報)。
- **新增涵蓋範圍(後 2 項,2026-09-04)**:從其餘 11 個沿用 `useFocusTrap` 的彈窗中,挑「風場戰情室(FleetOpsModal)」與「母港建設(PortModal)」兩個代表性樣本補上端到端 focus trap 驗證——兩者皆可從母港「設施」列一鍵開啟、無需前置遊戲狀態,面板內可聚焦元素數量在開局狀態下固定(風場戰情室:關閉✕+派員定檢+推進一天=3 個;母港建設:關閉✕+4 個設施升級鈕=5 個),各驗證開啟時 focus 移入面板、連續 Tab 侷限循環於面板內、`Esc` 關閉並歸還焦點給觸發的設施列。新增共用測試輔助函式 `checkModalFocusTrap()`。
- **新增涵蓋範圍(後 1 項,2026-09-05)**:再挑「營運趨勢(TrendsModal)」補上一筆——由母港左側「風場動態」面板(預設展開)的「📈 營運趨勢 · 賽後復盤」按鈕一鍵開啟,無需前置遊戲狀態。e2e 流程走到這裡時只完成過 1 筆工單(`FINISH_REPAIR` 才會 `pushHistory`),`data.history.length`(=1)未達 `TrendsModal` 顯示圖表所需的門檻 2,畫面固定停在「尚無足夠資料」提示、面板內僅關閉✕ 1 個可聚焦元素——補上先前兩個樣本(3 個/5 個可聚焦元素)未覆蓋的「單一可聚焦元素」邊界情境:Tab/Shift+Tab 皆應停留原地、不逃逸。沿用既有 `checkModalFocusTrap()`。
- **新增涵蓋範圍(後 1 項,2026-09-06)**:再挑「課程模式(CourseModal)」補上一筆——觸發鈕是頂欄「⚙」齒輪鈕,隨處可見、無需前置遊戲狀態。面板內可聚焦元素數量在開局狀態下固定(關閉✕ + 重播教學 + 獨立測驗模式 + 教師檢視 + 開放週次 −/＋(2)+ `COURSE_WEEKS` 18 週各 1 個「指派」鈕 + 匯入任務文字框 + 匯入並指派 + 情境包文字框 + 匯入情境包 = 28 個;e2e 測試環境為全新 `localStorage`、無已匯入情境包,故不含「移除」鈕),刻意挑一個元素數量遠多於先前樣本(1/3/5 個)的大面板,驗證 trap 在較長 Tab 序列(30 次)下仍正確循環回頭、不逃逸。沿用既有 `checkModalFocusTrap()`。
- **新增涵蓋範圍(後 1 項,2026-09-07)**:再挑「機具工坊」補上一筆——`FacilityModal` 是共用元件,依 `kind` 顯示 6 種不同內容(技師公會/機具工坊/船隊整備廠/風場拓展/圖鑑/排行);選擇 `kind="tool"`(機具工坊):由母港「設施」列一鍵開啟、無需前置遊戲狀態,面板內可聚焦元素為關閉✕ + 升級鈕 = 2 個,開局預算(8420 萬)遠高於首級升級費(100 萬)故升級鈕未 disabled(disabled 按鈕不進 tab 序,見 `a11y.ts` 的 `FOCUSABLE_SELECTOR`)——補上先前樣本(1/3/4/5/28 個)之間尚未覆蓋的「雙元素」情境。`FacilityModal` 的其餘 kind 中,`kind="tech"`(技師公會)候選名單以 `Math.random()` 產生、可聚焦元素數量不穩定,暫緩納入。沿用既有 `checkModalFocusTrap()`。
- **新增涵蓋範圍(後 1 項,2026-09-08)**:排查剩餘尚無 e2e 樣本的彈窗時,發現「風場建置 · 番外篇」(`ConstructionModal`)的階段選項卡(每階段 2 選 1、共 8 階段)只有滑鼠 `onClick`,未比照工單循環其餘卡片式互動(母港設施列/交易所備品卡/診斷測驗選項/SOP 步驟)補上 `role="button"`/`tabIndex`/`onKeyDown`(`onKeyActivate`)——是既有無障礙工作(2026-08-31)遺漏的一處缺口,鍵盤玩家完全無法操作此番外篇短戰役。本輪先在 `ConstructionModal.tsx` 補上,再新增 e2e 樣本:由母港「設施」列開啟「風場建置(番外篇)」,開局停在階段 0、面板內可聚焦元素為關閉✕ + 2 個選項卡 = 3 個。與先前樣本不同,本次**不沿用**共用的 `checkModalFocusTrap()`(該輔助函式只驗證 focus 不逃逸出面板,不驗證確實走訪了哪些元素——若選項卡未加上 `tabIndex`,Tab 只會在唯一的關閉✕上打轉,一樣「不逃逸」而空綠通過,測不出這處迴歸),改為逐步比對 `document.activeElement` 的 `role`/`aria-label`/`textContent`,確認 Tab 序列確實是「關閉✕ → 選項卡 1 → 選項卡 2 → (循環回)關閉✕」,並用鍵盤 `Enter` 實際選取一張選項卡、確認回饋文字揭曉(驗證 `onKeyActivate` 真的推進了遊戲狀態,不只是聚焦得到),最後 `Esc` 關閉並確認焦點歸還。
- **新增涵蓋範圍(後 1 項,2026-09-09)**:延續「補完各處遺漏的鍵盤操作缺口」的排查思路,這次盤點**登入畫面**(`LoginScreen.tsx`)——2026-08-31 的兩輪無障礙工作(工單循環鍵盤操作、全部彈窗 focus trap)皆聚焦於登入後的母港/工單循環畫面,完全未觸及登入畫面本身。逐一檢視後發現 7 處自訂卡片式互動(帳號清單列、切換模式連結共 4 處、訪客試玩、教師檢視入口)全都只有滑鼠 `onClick`,未補上 `role="button"`/`tabIndex`/`onKeyDown`——鍵盤玩家完全無法從登入畫面選擇既有帳號、切換登入模式、以訪客身分進場,或(尤其)不必先登入即開啟的「教師檢視入口」。本輪在 `LoginScreen.tsx` 一次補齊全部 7 處。新增 e2e 樣本挑「教師檢視入口」→ `TeacherModal`:此路徑不需任何前置遊戲狀態(登入畫面載入後即可觸發),且面板在「表單」狀態下可聚焦元素固定(關閉✕ + 班級碼輸入框 + 教師碼輸入框 + 查詢按鈕 = 4 個,不受雲端連線狀態或帳號資料影響)——同時補上 ROADMAP 明列尚缺 e2e 樣本的 `TeacherModal`。刻意用鍵盤 `Enter`(而非滑鼠 `.click()`)觸發開啟,驗證新補的 `onKeyActivate` 確實可運作,而非只是新增了屬性;開啟後驗證 focus 移入面板、連續 6 次 Tab 侷限循環於面板內、`Esc` 關閉並歸還焦點給「教師檢視入口」連結。
- **新增涵蓋範圍(後 2 項,2026-09-10)**:延續盤點剩餘無 e2e 樣本的彈窗(`OpsCenterModal`/`CaseFileModal`/`ProfileModal`/`ExamModal`)時,原想直接挑 `ExamModal`「開始頁」補樣本(先前(2026-09-07)已判定其可聚焦元素固定:關閉✕+10題+20題=3 個),但追查它「由 `CourseModal` 內『獨立測驗模式』鈕觸發、`App.tsx` 在同一事件處理常式內 `setShowCourse(false); setShowExam(true)` 兩個彈窗串接卸載/掛載」的先前已知懸案時,發現一處比「補哪個彈窗樣本」更根本的缺口:**`TopBar.tsx`(桌機頂欄)與 `MobileBar.tsx`(手機頂列)的全部自訂互動——共用 `Btn` 元件(語言切換/靜音/⚙/登出)、導覽分頁(母港/交易所/出海/維修)、個人檔案晶片——都只是滑鼠 `onClick` 的 `<div>`,既未比照工單循環其餘卡片式互動補上 `role="button"`/`tabIndex`/`onKeyDown`,連最基本的「滑鼠點擊後成為 `document.activeElement`」都不成立(純 `<div>`、無 `tabindex` 的元素滑鼠點擊不會取得焦點)**。這是 2026-08-31 兩輪無障礙工作(工單循環鍵盤操作、全部彈窗 focus trap)都聚焦於「彈窗內部」與「母港設施列」、完全遺漏「頂欄本身」的系統性缺口,鍵盤玩家原本完全無法從頂欄開啟課程模式、切換分頁、開個人檔案。更嚴重的是,這處缺口牽出一個**真實的焦點遺失迴歸**:由於 ⚙ 齒輪鈕滑鼠點擊不會取得焦點,`CourseModal`(由 ⚙ 開啟)的 `useFocusTrap` 記下的「開啟前焦點」其實是 `<body>`;串接開啟 `ExamModal` 並 `Esc` 關閉後,焦點便遺失回 `<body>`,鍵盤玩家會徹底失去目前游標位置(用 `focus()` 呼叫記錄實測證實:修復前 `Esc` 後只呼叫過 `body.focus()`/`span(✕).focus()`,從未呼叫到 ⚙ 本身)。本輪在 `TopBar.tsx`(共用 `Btn`、4 個導覽分頁、個人檔案晶片)與 `MobileBar.tsx`(4 個 `iconBtn`、個人檔案晶片、4 個導覽分頁)一次補齊全部同類元素。新增 2 項斷言:①鍵盤 `Enter` 觸發 ⚙ 開啟課程模式(驗證頂欄修復本身);②由課程模式「獨立測驗模式」鈕串接開啟 `ExamModal`,驗證開始頁 focus trap(關閉✕→10題→20題→循環回關閉✕)後 `Esc` 關閉,焦點精確歸還至 ⚙(用 `tagName`/`role`/`trim()` 後的 `textContent` 精確比對,刻意不沿用既有慣例的 `textContent.includes(triggerText)` 寬鬆比對——若焦點真的遺失到 `<body>`,`body.textContent` 幾乎必然仍「包含」觸發文字,會讓這處迴歸被寬鬆比對誤判為通過,測不出來)(合計 **25** 項全過)。
- **新增涵蓋範圍(後 1 項,2026-09-11)**:延續盤點剩餘無 e2e 樣本的彈窗(`OpsCenterModal`/`CaseFileModal`/`ProfileModal`)之一,挑選 `ProfileModal`(個人檔案)。此彈窗大部分內容隨作答資料變動(知識點掌握度 chips/雷達圖只在 `totalAnswered(m)>0` 時出現、`MistakeLog` 錯題本只在 `data.mistakes` 非空時才有可互動元素),但成就牆卡片(`ACHIEVEMENTS.map`)與數據格皆是純 `<div>`,未補 `role="button"`/`tabIndex`,本就不進 tab 序——故實際可聚焦元素數只取決於「是否已作答/答錯過」,而非成就解鎖進度或紀錄內容。在測試流程中,於「調度中心」彈窗驗證完畢、尚未接下首筆工單前插入本樣本(此時 `data.mastery`/`data.mistakes` 皆為初始空值,後面「連續故意答錯」等測試才會產生作答紀錄),故面板內可聚焦元素固定為:關閉✕ + 減少動態切換鈕 = 2 個。觸發元件為頂欄(`TopBar`/`MobileBar`,`.first()` 取 `TopBar` 先掛載者)個人檔案晶片(先前僅在 2026-09-10 驗證過其鍵盤可達性,尚未有實際彈窗內容的 e2e 樣本),用鍵盤 `Enter` 觸發開啟,驗證 focus 落在關閉✕→Tab 至減少動態鈕(預設 OFF,文字為「關閉」)→再 Tab 循環回關閉✕,`Esc` 關閉後焦點歸還晶片(合計 **26** 項全過)。
- **新增涵蓋範圍(後 1 項,2026-09-12)**:再挑「案例檔(`CaseFileModal`)」補上一筆——先前多輪盤點(2026-09-05/06/07 等)都因「案例卡片依 Tier/隨機案例變動,可聚焦元素數量不穩定」為由暫緩納入。本輪查核 `src/state/caseStudies.ts` 原始碼後發現這個顧慮並不成立:`casesForTier(tier)` 是純函式(`CASE_STUDIES.filter((c) => c.minTier <= tier)`),依 `tierOf()` 過濾而非隨機抽取;而全部 20 則案例的 `minTier` 最低為 2,本測試流程全程 `tierOf()` 皆為 1(開局 `generationMWh`/`missionsDone` 皆遠低於 Tier 2 門檻、`farmsOwned=1`、`campaignIndex<2`),故 `casesForTier(1)` 恆為空陣列,面板固定停在「目前層級尚無解鎖案例」提示,可聚焦元素只有關閉✕ 1 個——觸發元件為母港「設施」列的 `FacRowMini`「📁 案例檔」,一鍵開啟、無需前置遊戲狀態。與「營運趨勢」彈窗同屬「單一可聚焦元素」邊界情境,但成因不同(前者是 `history` 長度未達門檻,此為 tier 過濾)。沿用既有 `checkModalFocusTrap()`(合計 **27** 項全過)。純測試新增,無 app 程式碼變動。
- **新增涵蓋範圍(後 1 項,2026-09-13)**:再挑「自由營運中心(`OpsCenterModal`)」補上一筆——先前多輪盤點都以「判斷任務/案例演練隨機抽題,選項數隨模板變動,需先鎖定 `Math.random()`」為由暫緩。本輪查核 `src/ui/OpsCenterModal.tsx` 原始碼時發現一處比「鎖定 `Math.random()`」更根本的缺口:判斷任務/案例演練的選項卡(`resolve(i, c)` 對應的 `<div>`)全都只有滑鼠 `onClick`,未比照工單循環其餘卡片式互動補上 `role="button"`/`tabIndex`/`onKeyDown`——鍵盤玩家完全無法在此彈窗作答,是 2026-08-31 兩輪無障礙工作遺漏的又一處系統性缺口。本輪先在 `OpsCenterModal.tsx` 補上(手法與 `RepairScreen.tsx` 診斷測驗選項一致),再新增 e2e 樣本:`makeDraw()` 以 `Math.random() < CASE_DRILL_PROB(0.24)` 決定任務/案例,`generateTask()` 的模板索引/機組編號亦吃 `Math.random()`;暫時覆寫固定回傳 `0.999999`(恆落入判斷任務分支,且 `floor(0.999999 * TASKS.length)` 對任何合理長度恆為最後一個索引)鎖定抽到 TASKS 最後一筆模板「順勢限電維修」(cat D、2 個選項)。除錯時發現 `OpsCenterModal` 是 `lazy()` 元件,第一次開啟要等動態 import 完成才真正掛載並呼叫 `makeDraw()`,晚於 `click()` 這個 tick——若比照既有 `rush()` 測試在 `click()` 後立刻還原 `Math.random`,面板真正掛載時已還原成真隨機(實測直接證實抽到非預期模板),改為撐到 `dialog.waitFor({state:"visible"})` 之後才還原即穩定重現。驗證鍵盤 Tab 序列(關閉✕ → 🔬進階檢測「解鎖」原生 `<button>`(開局預算 8420 萬遠高於 `DIAG_COST` 800 萬,未 disabled,故進入 tab 序)→ 選項 1 → 選項 2 → 循環回關閉✕,共 4 個可聚焦元素)、鍵盤 `Enter` 選取正解後回饋文字與「下一個狀況」按鈕出現、`Esc` 關閉並歸還焦點(合計 **28** 項全過)。`OpsCenterModal.tsx`(app 程式碼)+ `test/e2e.mjs`(測試)皆有變動,`public/sw.js` 快取版本 v13→v14。
- **新增涵蓋範圍(後 1 項,2026-09-14)**:先前(2026-09-07/09-10)兩輪盤點都以「`ExamModal` 由 `buildExam(Date.now(), n)` 抽題,真實時間戳記為種子,題目/選項內容不可預期」為由,只補了「開始頁」(關閉✕+10題+20題=3 個可聚焦元素)的樣本,「作答頁」/「結果頁」因抽題內容變動一直暫緩。本輪查核 `src/state/exam.ts` 後發現 `buildExam` 對「同一個種子」是完全決定性的純函式(已由 `test/run.mjs` 單元測試證實),只要在點擊「10 題」之前把 `window.Date.now` 暫時鎖定為固定時間戳,選出的 10 題內容就完全可預期(不像 `OpsCenterModal` 需要鎖定 `Math.random()`)——離線用同一份 esbuild bundle 先跑一次 `buildExam(固定種子, 10)` 算出全部題目與選項,事先決定每題要點選哪個索引(第 1、6 題故意選第 2 個選項答錯,其餘 8 題皆選第 1 個選項答對),讓最終成績(8/10=80%「良好 B」、各類別對錯、2 題錯題覆盤)全部可預期。新增樣本驗證:鎖定種子後逐題比對題號「N / 10」與題目標題、依表答題直到自動進入結果頁、結果頁的總分(80%)/等第(良好 B)/答對數(8/10)/各類別對錯列(7 類、correct/n 緊鄰標籤無間隔渲染)/錯題覆盤區塊(顯示 2 題、揭示各自正解文字)皆與離線算得的預期值精確相符,再點擊「再測一次」確認正確重置回開始頁、`Esc` 關閉卸載(合計 **29** 項全過)。純測試新增(`test/e2e.mjs`),無 app 程式碼變動,`public/sw.js` 快取版本免動。
- **有效性驗證**:
  - (沿用,2026-09-01)刻意暫時移除 `useFocusTrap.ts` 的 Tab 循環邏輯後重跑,測試如預期在「第 6 次 Tab 後 focus 逃出了彈窗」失敗,證明測試確實會抓到迴歸,而非空跑。
  - (沿用,2026-09-02)刻意暫時拿掉診斷測驗選項的 `onKeyDown={onKeyActivate(pickOption)}`,重跑後如預期在「維修診斷測驗」步驟逾時失敗(鍵盤 `Enter` 不再能選取選項、完工按鈕永遠停用),證明新增的鍵盤操作斷言確實有效。
  - (沿用,2026-09-03)刻意暫時把 `RUSH_SOP` reducer 的 `steps: rp.steps.map(() => true)` 改回 `steps: rp.steps`(模擬「加班搶修沒有真的完成步驟」的迴歸),重跑後如預期連鎖失敗 3 項(吃緊提示未解除 → 完工按鈕逾時未出現 → 無法完工),證明新增斷言確實會抓到迴歸。
  - (沿用,2026-09-04)刻意暫時把 `useFocusTrap.ts` 的 Tab 處理短路成必定 `return`(略過 trap 邏輯)後重跑,測試如預期連鎖失敗 3 項(既有的調度中心 Tab 迴歸 + 新增的風場戰情室/母港建設兩項,母港建設甚至因 focus 逃逸擋住點擊而逾時),證明新增斷言確實會抓到迴歸;還原後 18 項全過。
  - (沿用,2026-09-05)同樣把 `useFocusTrap.ts` 的 Tab 處理短路成必定 `return` 後重跑,測試如預期連鎖失敗 4 項(既有的調度中心/風場戰情室 Tab 迴歸 + 新增的營運趨勢彈窗,母港建設與營運趨勢兩者更因 focus 逃逸擋住點擊而逾時),證明新增斷言確實會抓到迴歸;還原後 19 項全過。
  - (沿用,2026-09-06)同樣把 `useFocusTrap.ts` 的 Tab 處理短路成必定 `return`(在 `if (!items.length) return;` 之後插入)後重跑,測試如預期連鎖失敗 5 項(既有的調度中心/風場戰情室 Tab 迴歸 + 新增的課程模式彈窗,母港建設/營運趨勢/課程模式三者更因 focus 逃逸擋住後續點擊而逾時),證明新增斷言確實會抓到迴歸而非空跑;還原後 20 項全過。
  - (沿用,2026-09-07)同樣把 `useFocusTrap.ts` 的 Tab 處理短路成必定 `return`(`if (e.key === "Tab") return;` 插在 `onKeyDown` 開頭)後重跑,測試如預期連鎖失敗 6 項(既有的調度中心/風場戰情室 Tab 迴歸 + 新增的機具工坊彈窗,母港建設/營運趨勢/課程模式/機具工坊四者更因 focus 逃逸擋住後續點擊而逾時),證明新增斷言確實會抓到迴歸而非空跑;還原後 21 項全過。
  - (沿用,2026-09-08)刻意暫時把 `ConstructionModal.tsx` 選項卡的 `role="button"`/`tabIndex`/`onKeyDown` 改回還沒補的樣子(只留 `onClick`)後重跑,測試如預期在「第 1 次 Tab 後應落在第一張階段選項卡」失敗(1 項)——證明新增斷言確實會抓到「選項卡鍵盤不可達」這處迴歸,而非只驗證泛用的 focus-不逃逸而空綠通過;還原後 22 項全過。
  - (沿用,2026-09-09)刻意暫時把 `LoginScreen.tsx`「教師檢視入口」的 `role="button"`/`tabIndex`/`onKeyDown` 改回還沒補的樣子(只留 `onClick`)後重跑,新增斷言如預期逾時失敗(`locator.press: Timeout 30000ms exceeded`,因元素不再符合 `[role="button"]` 選擇器、鍵盤無法觸發),其餘 22 項不受影響——證明新斷言精準指向這處迴歸;還原後 23 項全過。
  - (沿用,2026-09-10)刻意暫時把 `TopBar.tsx`/`MobileBar.tsx` 本輪的修正全數還原(`git stash` 兩檔改動,只留 `test/e2e.mjs` 的新斷言)後重跑,新增的 2 項斷言如預期連鎖失敗——① 鍵盤 `Enter` 觸發 ⚙ 逾時(`locator.press: Timeout 30000ms exceeded`,元素不再符合 `[role="button"]` 選擇器、鍵盤完全無法觸發),② 因課程模式未能開啟,串接開啟 `ExamModal` 的斷言連帶逾時(`locator.click: Timeout 30000ms exceeded`,等不到「獨立測驗模式」鈕出現)——證明新增斷言確實會抓到「頂欄鍵盤不可達」這處迴歸而非空跑,其餘 23 項不受影響;還原修正後 25 項全過。另外在修復前(僅補 `TopBar.tsx`/`MobileBar.tsx` 但尚未確認)以 `HTMLElement.prototype.focus` 埋樁記錄呼叫序列實測證實:滑鼠點擊 ⚙ 齒輪鈕(修復前,純 `<div>` 無 `tabindex`)不會使其成為 `document.activeElement`,導致 `CourseModal`→`ExamModal` 串接關閉後,`Esc` 只呼叫過 `body.focus()`,焦點確實遺失到 `<body>`,而非玩家實際點擊的 ⚙——這正是本輪修正 `TopBar.tsx`/`MobileBar.tsx` 所解決的根本問題。
  - (沿用,2026-09-11)刻意暫時把 `useFocusTrap.ts` 的 Tab 處理短路成必定 `return`(`if (e.key === "Tab") return;` 插在 `onKeyDown` 開頭)後重跑,新增的 `ProfileModal` 斷言如預期失敗(`locator.waitFor: Timeout 5000ms exceeded`——由於前一個「調度中心」彈窗的 Esc 焦點歸還鏈也受同一處短路影響而連鎖失敗,`ProfileModal` 開啟後 dialog 選擇器等不到預期狀態),連帶造成後續多個彈窗測試逾時(`4 passed, 22 failed`),證明新增斷言確實會參與抓到這處全域迴歸而非獨立空跑;還原後 26 項全過。
  - (沿用,2026-09-12)同樣把 `useFocusTrap.ts` 的 Tab 處理短路成必定 `return` 後重跑,新增的「案例檔」斷言與其餘多項斷言如預期連鎖失敗(4 passed, 23 failed——因某個彈窗的 Esc 焦點歸還鏈斷裂、背景遮罩未卸載,阻擋後續設施列點擊而逐一逾時),證明新增斷言確實參與抓到這處全域迴歸而非獨立空跑;還原後 27 項全過。
  - (沿用,2026-09-13)刻意暫時把 `OpsCenterModal.tsx` 本輪新補的 `role="button"`/`tabIndex`/`onKeyDown` 還原(`git stash` 該檔改動,只留 `test/e2e.mjs` 新斷言與 `public/sw.js`)後重跑,新增斷言如預期在「第 2 次 Tab 後應落在第一個判斷選項」失敗(選項卡未加 `tabIndex`,Tab 序列變短、提早循環回關閉✕),其餘 27 項不受影響,證明新增斷言確實會抓到這處鍵盤操作缺口而非空跑;還原修正後 28 項全過。
  - (新增,2026-09-14)刻意暫時把 `src/state/exam.ts` 的 `gradeExam` 計分公式改成 `Math.round((correct/total)*100) + 5`(模擬「計分算錯」的迴歸)後重跑,新增斷言如預期逾時失敗(等不到「80%」文字,實際顯示的是被灌水後的分數),並連鎖拖累下一項「自由營運中心」測試逾時(因 `ExamModal` 卡在結果頁未關閉、遮罩持續攔截後續點擊)——證明新增斷言確實會抓到計分邏輯的迴歸而非空跑;還原後 29 項全過。
- **CI**:`.github/workflows/ci.yml` 的 `e2e` job(`npm ci` → `npm run build` → `npx playwright install --with-deps chromium` → `npm run e2e`)不變,與既有 `check` job(typecheck/test/build)並行,兩者皆需全綠才能合併。
- **結果**:`29 passed, 0 failed`。
- **已知限制/後續**:`ExamModal` 已於本輪(2026-09-14)補上「作答頁」/「結果頁」樣本(固定 `Date.now()` 種子、10 題全部作答完的完整流程),但僅覆蓋「10 題」長度、單一固定種子下的一組題目組合,「20 題」選項與其他種子下的題目排列尚未覆蓋;彈窗 focus trap/鍵盤操作目前所有 12 個彈窗皆至少有 1 個 e2e 樣本(`TeacherModal` 已於 2026-09-09 透過登入畫面的「教師檢視入口」補上「表單」狀態的樣本,「查詢結果」狀態因表格列數/掌握度鑽取展開內容隨學生存檔資料變動,尚未納入;`ProfileModal` 已於 2026-09-11 補上「0 作答/0 錯題」狀態的樣本,「已作答/有錯題」狀態因掌握度 chips/錯題卡片數量變動,尚未納入;`CaseFileModal` 已於 2026-09-12 補上「尚無解鎖案例」狀態的樣本,若日後測試流程推進到 Tier ≥2 則案例卡片數量會隨解鎖進度變動,屆時需重新評估;`OpsCenterModal` 已於 2026-09-13 補上「判斷任務、尚未作答」狀態的樣本,案例演練(`kind:"case"`)分支與已作答後再抽題狀態尚未覆蓋),`FacilityModal` 則僅 `kind="tool"`(機具工坊)一種內容有樣本,其餘 5 種(技師公會/船隊整備廠/風場拓展/圖鑑/排行)未覆蓋(由單元測試保證邏輯一致)——其中 `FacilityModal`(`kind="tech"` 技師公會候選名單以 `Math.random()` 產生)可聚焦元素數量仍不穩定,需先鎖定 `Math.random()` 抽題分支的測試時間點才適合納入;覆蓋情境仍限於「首筆工單(齒輪箱過熱,mechanical 科別)」,尚未涵蓋大型組件(重大故障轉多回合大修)、審慎返港再規劃(`#carry`,含跨日 `REPLAN_RETURN` 後的天氣重擲,較不適合純 UI 端到端斷言,且會與海象隨機性耦合)等分支路徑。

---

## 結論

全功能邏輯(167 測試)、型別、建置、平衡模擬、併發壓力測試、Playwright UI 迴歸(29 測試)**全部通過**;併發鎖證實有效。唯一無法在沙盒內跑的是線上後端實打(代理限制),需於本機執行。**系統目前處於可發布狀態。**

> 維護備註:新增功能時請同步在 `test/run.mjs` 補對應測試(邏輯/reducer)或 `test/e2e.mjs`(UI 流程),並更新本報告的「最近一次執行」日期與對應涵蓋表。
